"use client"

import { PublicProfileError } from "@/components/public-profile-page"

export default function TeamProfileError() {
  return <PublicProfileError message="Team profile data could not be loaded right now." />
}
